#include <stdio.h>
int main()
{
    int N;
    scanf("%d", &N);
    if (N <= -1 )
    {
        printf("Non Zero");
    }
    else if (N > 0)
    {
        printf("Non Zero");
    }
    else
    {
        printf("Zero");
    }
    return 0;
}